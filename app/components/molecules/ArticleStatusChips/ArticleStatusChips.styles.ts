import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flexDirection: 'row',
    flexWrap: 'wrap',
    marginBottom: 4,
  },
  chip: {
    marginRight: 6,
    marginBottom: 4,
    height: 24,
  },
  text: {
    fontSize: 11,
  },
});
